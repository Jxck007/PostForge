#!/usr/bin/env bash
set -euo pipefail

# Lightweight reminder hook.
# Does not block. It only prints guardrails.

echo "PostForge guardrails: no LinkedIn auto-posting, no scraping, no API keys in frontend, run build after edits."
